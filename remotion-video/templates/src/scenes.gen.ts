// Registry of imported project components for component-mode (B) scenes.
// The scaffold overwrites this file from the manifest. Empty by default so
// capture-only videos build with no project imports.
//
// Example (component mode):
//   import { DashCard } from "../../NarraNexus-1/frontend/src/components/dashboard/DashCard";
//   export const componentRegistry: Record<string, React.FC> = { "dash-card": DashCard };

export const componentRegistry: Record<string, React.FC> = {};
